---
- hosts: "{{ any }}"
  connection: ssh
  user: ec2-user
  sudo: yes
  gather_facts: yes
  tasks: 
  - name: Install HTTPD
    yum: pkg=httpd state=absent
    tags: 
      - httpdtag
  - name: 
    yum: pkg=nginx state=absent
    tags: 
     - nginxtag
  - name: Install TELNET
    yum: pkg=telnet state=absent
    tags: 
      - telnettag
